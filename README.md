Python 3.11.2

  python3 -m venv .venv
  source .venv/bin/activate
  pip install -r requirements.txt
  python -m tool.main

```
source .venv/bin/activate

./runpy serial_plotter_terminal.py --port /dev/ttyACM0 --baud 115200 --window 60 --quick-file planetJoint.json 
```
